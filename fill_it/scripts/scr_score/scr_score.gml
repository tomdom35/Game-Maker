for(i = 0; i < num_spots; i++){
	for(j = 0; j < num_spots; j++){
		x_val = i*16
		y_val = (j*16) + grid_start_y
		block = instance_position(x_val, y_val, obj_block)
		if(block){
			if(block.done){
					
			}
		}
	}
}