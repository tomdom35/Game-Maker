//Colliding block in horizontal direction
h_block = instance_place(x + hspeed, y, obj_block)

//Colliding block in vertical direction
v_block = instance_place(x, y + vspeed, obj_block)

//Colliding enemy in horizontal direction
h_enemy = instance_place(x + hspeed, y, obj_enemy)

//Colliding enemy in vertical direction
v_enemy = instance_place(x, y + vspeed, obj_enemy)

//Colliding player in horizontal direction
h_player = instance_place(x + hspeed, y, obj_player)

//Colliding player in vertical direction
v_player = instance_place(x, y + vspeed, obj_player)